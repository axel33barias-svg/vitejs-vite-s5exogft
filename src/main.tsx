import React from "react";
import ReactDOM from "react-dom/client";
import DiceRoller from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <DiceRoller />
  </React.StrictMode>
);